{
  "$schema": "https://raw.githubusercontent.com/martinring/tmlanguage/master/tmlanguage.json",
  "name": "PLang",
  "patterns": [
    {
      "include": "#keywords"
    },
    {
      "include": "#values"
    },
    {
      "include": "#types"
    },
    {
      "include": "#strings"
    }
  ],
  "repository": {
    "keywords": {
      "patterns": [
        {
          "name": "keyword.control.plang",
          "match": "\\b(pop|push|add|print|cast|dup)\\b"
        }
      ]
    },
    "values": {
      "patterns": [
        {
          "name": "constant.character.plang",
          "match": "\\b[0-9]+\\b"
        }
      ]
    },
    "types": {
      "patterns": [
        {
          "name": "support.type.plang",
          "match": "\\b(int|string)\\b"
        }
      ]
    },
    "strings": {
      "name": "string.quoted.double.plang",
      "begin": "\"",
      "end": "\"",
      "patterns": [
        {
          "name": "constant.character.escape.plang",
          "match": "\\\\."
        }
      ]
    }
  },
  "scopeName": "souce.p"
}
